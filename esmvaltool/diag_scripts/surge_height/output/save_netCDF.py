def save_netCDF(dates,stat,coords,lons,lats):
	import netCDF4
	# define coordinates, i.e. time, lat & lon (only for map)
	#if coastal_map:
	# create varibales & their attributes
	#
	# write netCDF-file
		
