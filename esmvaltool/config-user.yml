###############################################################################
# User's configuration file for the ESMValTool
###############################################################################
---

# Diagnostics create plots? [true]/false
write_plots: true
# Diagnositcs write NetCDF files? [true]/false
write_netcdf: true
# Set the console log level debug, [info], warning, error
log_level: info
# verbosity is deprecated and will be removed in the future
# verbosity: 1
# Exit on warning? true/[false]
exit_on_warning: false
# Plot file format? [ps]/pdf/png/eps/epsi
output_file_type: pdf
# Destination directory
output_dir: /group_workspaces/jasmin2/ukesm/till/esmvaltool
# Use netCDF compression true/[false]
compress_netcdf: false
# Save intermediary cubes in the preprocessor true/[false]
save_intermediary_cubes: false
# Remove the preproc dir if all fine
remove_preproc_dir: false
# Run at most this many tasks in parallel null/[1]/2/3/4/..
# Set to null to use the number of available CPUs.
# Make sure your system has enough memory for the specified number of tasks.
# originally 8, but that led to memory fail
max_parallel_tasks: 8

rootpath:
  # Rootpath to CMIP5 data
  CMIP5: /badc/cmip5/data/cmip5/output1
  CMIP6: /badc/cmip6/data/CMIP6/CMIP
  # Rootpath to OBS data
  OBS: /group_workspaces/jasmin4/esmeval/obsdata-v2/
  #     /home/users/valeriu/esmvaltool_workshop_21Jan2019/OBS_DATA
  # Default
  default: / 
  # /home/users/valeriu/esmvaltool_workshop_21Jan2019/OBS_DATA
  RAWOBS: TEST_INPUT

# Directory structure for input data: [default]/BADC/DKRZ/ETHZ/etc
# See config-developer.yml for definitions.
drs:
  CMIP5: BADC
  CMIP6: BADC 
